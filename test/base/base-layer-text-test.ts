import { expect } from "chai";
import { LayerTextCanvas } from "../../src/ts/base/base-layer-text";

describe("LayerTextCanvas", () => {
  let textCanvas: LayerTextCanvas;

  beforeEach(() => {
    textCanvas = new LayerTextCanvas();
  });

  context("constructor", () => {});
});
